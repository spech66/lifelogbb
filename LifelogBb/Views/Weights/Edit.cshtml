@model LifelogBb.Models.Weights.EditWeightViewModel

@{
    ViewData["Title"] = "Weight";
    ViewData["Pretitle"] = "Edit";
}

<div class="container-xl">
    <div class="row row-cards">
        <div class="col-12">
            <form asp-action="Edit" class="card">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group mb-3">
                        <label asp-for="BodyWeight" class="control-label mb-1"></label>
                        <input asp-for="BodyWeight" class="form-control form-control-lg" type="number" step="0.1" />
                        <span asp-validation-for="BodyWeight" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Height" class="control-label mb-1"></label>
                        <input asp-for="Height" class="form-control form-control-lg" />
                        <span asp-validation-for="Height" class="text-danger"></span>
                    </div>
                    <input type="hidden" asp-for="Id" />
                </div>
                <div class="card-footer text-end">
                    <div class="d-flex">
                        <a class="btn btn-secondary" asp-action="Index"><i class="fas fa-arrow-circle-left icon"></i>Back</a>
                        <button type="submit" value="Save" class="btn btn-primary ms-auto"><i class="fas fa-pen icon"></i>Submit</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
